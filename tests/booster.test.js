test('booster applies migrations and seeds on the default database', () => {

})

test('booster applies migrations and seeds on the test database', () => {

})

test('booster applies migrations and seeds of specified knexfile', () => {

})
